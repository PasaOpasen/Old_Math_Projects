# ƒоступны sin, cos, Im, Re, ch, sh, exp, ln, sqrt, abs, sqr, cube,
# ќперации + - / *
# ћожно вписывать действительные константы,
# числа pi, I (мнима€ единица),
# формировать выражени€ в скобках
#

‘ункции, у которых надо найти корни:

z*z
z+2
(z-2)*(z-4-5*I)*z*(z-1,3)^3
sh(z)+cos(z)
(z-3)*(z+2)+exp(z)*(z+2)
Re(z)+Im(sh(z)*sqrt(z/10,231)-2-4*I)


# ѕараметры поиска:
# границы пр€моугольника, в котором случайным образом выбираютс€ 3 начальных значени€ дл€ метода ћюллера
# точность вычислений
# число попыток дл€ каждой функции
#
# при некоторых начальных положени€х точек, метод может ломатьс€, потому что две точки из трЄх
# окажутс€ очень близко друг к другу и нельз€ будет построить параболу
#
# также метод сходитс€ к случайному корню, поэтому дл€ отыскани€ многих корней надо делать много попыток на разных начальных данных
#
ѕараметры:
xmin= -4
xmax= 4
ymin= -19
ymax= 3,564
точность= 1e-12
максимальное_число_попыток= 50
